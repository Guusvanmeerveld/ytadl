{
	"private": "true",
	"workspaces": [
		"packages/base"
	],
	"scripts": {
		"build:base": "yarn --cwd packages/base build",
		"test:base": "yarn --cwd packages/base test",
		"lint:base": "yarn --cwd packages/base lint",
		"build:docs": "typedoc --entryPointStrategy expand packages/base/src/index.ts --tsconfig packages/base/tsconfig.json"
	}
}
